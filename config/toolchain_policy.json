{
  "schema": "NREF_P1_COMPAT_TOOLCHAIN_POLICY_v1.1",
  "classification": "P1-COMPAT CANDIDATE \u2014 NATIVE SOURCE / MODERN TOOLCHAIN COMPATIBILITY RUN",
  "matlab_release": "R2023b",
  "simulink_required": true,
  "runner_family": "windows-2022",
  "expected_c_mex_compiler": {
    "manufacturer": "Microsoft",
    "name_regex": "Microsoft Visual C\\+\\+ 2022.*\\(C\\)|Microsoft Visual C\\+\\+ 2022",
    "family": "Microsoft Visual C++ 2022 / Visual Studio 2022 Build Tools",
    "selection_method": "Enumerate mex.getCompilerConfigurations('C','Installed'); require exactly one installed configuration whose Language is C; require that sole C configuration to match the Microsoft Visual C++ 2022 family. Inspect mex.getCompilerConfigurations('C','Selected') before mutating compiler state. If exactly one selected C configuration already matches the sole candidate by semantic compiler identity, accept it without mex -setup. Otherwise execute documented 'mex -setup C' once as a recovery selection path, re-read Selected, and require semantic compiler identity to match the sole candidate. MexOpt is recorded as configuration provenance and is not a compiler identity field.",
    "fail_if_unavailable": true,
    "ambiguity_policy": "FAIL_IF_MORE_THAN_ONE_INSTALLED_SUPPORTED_C_CONFIGURATION",
    "semantic_identity_fields": [
      "Name",
      "Manufacturer",
      "Language",
      "Version",
      "Location"
    ],
    "location_comparison": "WINDOWS_CASE_INSENSITIVE_NORMALIZED_SEPARATORS_TRIM_TRAILING_SEPARATOR",
    "configuration_provenance_fields": [
      "MexOpt"
    ],
    "mexopt_policy": "RECORD_PATH_EXISTENCE_AND_SIZE_FOR_PROVENANCE_DO_NOT_USE_AS_COMPILER_IDENTITY",
    "preselected_match_action": "ACCEPT_WITHOUT_MEX_SETUP",
    "recovery_selection_action": "MEX_SETUP_C_IF_SELECTED_MISSING_OR_SEMANTIC_MISMATCH",
    "configuration_descriptor_fields": [
      "ShortName"
    ]
  },
  "historical_runtime_reproduction": false,
  "forbidden": "Do not emulate Microsoft SDK 7.1 and do not modify NASA source to satisfy modern compiler compatibility."
}
