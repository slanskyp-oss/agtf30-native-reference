# P1 tooling classification

Primary exact-source run:

**AGTF30 `a1521...` + T-MATS v1.3.0 `56066...` + MATLAB R2023b + explicitly selected Microsoft Visual C++ 2022 C MEX toolchain**

Classification:

**P1-COMPAT CANDIDATE — NATIVE SOURCE / MODERN TOOLCHAIN COMPATIBILITY RUN**

It is **not** a historical MATLAB-2015a/T-MATS-era runtime reproduction.

If exact v1.3.0 builds and runs unmodified, that is native-source evidence under the recorded modern toolchain. If it fails, classify the failure by execution stage; a tooling/compatibility failure is not a physical engine conclusion.
